import { ServicePerformedPipe } from './service_performed.pipe';

describe('ServicePerformedPipe', () => {
  it('should be defined', () => {
    expect(new ServicePerformedPipe()).toBeDefined();
  });
});
